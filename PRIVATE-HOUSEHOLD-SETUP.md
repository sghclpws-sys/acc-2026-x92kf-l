# 비공개 가계부 요약 연결

## 현재 상태

- 실제 월별 데이터는 GitHub `household.json`에 쓰지 않는다.
- `index.html`의 설정 → 통합 Advisor 월별 요약에서 미리보기 후 Firebase에 저장한다.
- 저장 경로: `gaegebu/main/summaries/YYYY-MM`.
- 가계부 AI는 공통 월별 계산에 기존 가맹점별 집계를 추가해 사용한다.
- 통합 요약에는 거래 원문, 가맹점, 거래 메모, 개인별 수입, UID, API 키를 넣지 않는다.
- 수입 또는 대출이자 입력 누락은 0이 아닌 null로 표현한다.
- 요약은 카드 거래와 대출이자 범위의 잠정치다. 현금 지출 전체/투자 납입까지 반영한 저축률로 해석하지 않는다.
- 현재 소비 계산식 `amount - settlement`를 유지한다. 환불을 음수 거래와 정산 양쪽에 중복 입력한 경우까지 자동 판별하지 않는다.

## 서버 적용 전에 필요한 확인

1. Firebase 관리 계정으로 현재 Firestore 규칙을 읽고 보관한다. 이 저장소의 규칙으로 전체 프로젝트 규칙을 무조건 덮어쓰지 않는다. 다른 앱이 사용하는 경로가 있으면 보존한다.
2. `gaegebu/main` 문서에 transactions, rules, incomeData, cashData가 보존돼 있는지 확인한다. 특히 HTML에만 있었던 거래·수입과 서버 데이터를 대조한다. 빈 서버를 브라우저 캐시로 자동 덮어쓰는 기능은 제거했다.
3. 제공된 두 UID가 실제 두 가구 구성원 계정인지 확인한다.
4. `firestore.rules`의 해당 가계부/조언/요약 경로 규칙을 적용한다. 넓은 경로에 기존 allow 규칙이 있으면 좁은 규칙을 우회할 수 있으므로 함께 검토한다.
5. 규칙 시뮬레이터에서 비로그인 및 다른 UID의 읽기/쓰기를 거부하고, 지정한 두 UID는 허용하는지 확인한다. 거래, 조언(_config 포함), 요약을 각각 검사한다.
6. 검증 후 관리자 콘솔에서 `security/householdSummary` 문서를 만들고 `rulesVersion` 문자열을 `household-uids-v1`로 설정한다. 이 확인 문서는 클라이언트가 변경할 수 없다. 확인 문서가 없으면 앱의 요약 저장은 중단된다. 이 문서는 운영상 적용 확인 표시이며 보안 규칙 자체를 대신하지 않는다.
7. 공개 HTML 배포 후 두 계정의 로그인, 기존 거래, 월별 요약 재저장, 비로그인 거부를 확인한다.

## 복원과 공개 이력

변경 전 HTML 원본은 저장소 밖에 보관되어 있다:

`C:\Users\Public\Documents\ESTsoft\CreatorTemp\household-private-backup-20260913-235650`

이 백업에는 개인정보가 있으므로 Git에 추가하거나 공개 업로드하지 않는다. 운영 서버 데이터 확인 전까지 보관한다. 이는 암호화된 백업이 아니므로 로컬 PC 접근 권한도 고려한다.

두 구버전 가계부 HTML은 로그인 앱으로 연결되는 페이지로 교체했다. 과거 PWA 가계부 캐시는 서비스 워커 갱신 시 제거되며, 새 워커는 HTML/데이터를 캐시하지 않는다. 브라우저 localStorage의 기존 백업은 복원을 위해 삭제하지 않았으며, 권한 확인에 실패했을 때 이를 자동으로 불러오지 않는다.

로컬 파일 수정만으로 이미 공개된 Git 이력, 기존 브라우저 캐시, 외부 복사본은 없어지지 않는다. 공개 이력 삭제/강제 푸시는 별도 검토가 필요하며 이번 작업에서 실행하지 않았다.

## 서버 적용 확인

서버 규칙을 게시하고 security/householdSummary의 rulesVersion을 household-uids-v1로 저장했다. Firebase 규칙 플레이그라운드에서 요약 문서 읽기는 비로그인/허용 목록 밖 UID에 대해 거부, 지정한 두 UID에 대해 허용되는 것을 확인했다. 지정한 UID의 보안 확인 문서 생성도 거부됐다. 기존 거래와 수입은 수정하지 않았다. 이 검증은 시뮬레이션이며 실제 앱 로그인 후 저장 확인은 별도다.

## 후속 통합 Advisor

통합 Advisor는 인증된 Firebase 읽기 또는 서버에서 허용된 요약 경로만 읽어야 한다. Firebase Admin SDK는 보안 규칙을 우회하므로 사용하는 서버에서도 요청자 권한과 허용 경로를 검증해야 한다. 공개 JSON 내보내기, 정기 브리핑 파이프라인 연결은 구현하지 않았다.

## 로컬 검증

`node --test tests/household-summary.test.cjs`

환불/정산 합계, 누락값 처리, 거래 개인정보 제외, 가맹점 AI 유지, HTML 스크립트 구문, 구버전 데이터 제거를 검사한다. 실제 Firebase 권한 검증은 위 서버 절차가 별도로 필요하다.
