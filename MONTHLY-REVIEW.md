# 월말 결산과 예산

월별 요약의 ‘우리의 월말 결산’에서 월평균·이번 달·평균 대비 증감, 예산, 다음 달 목표와 두 사람의 회고를 관리한다. 기존 소비 관리 인사이트에서 바로 이동할 수 있다.

## 계산 기준

- 금액은 카드 결제액에서 정산금을 뺀 순지출이다. 이자는 이 예산에서 제외한다.
- 여행은 총예산에서 제외하되 별도 실적과 원인·목표를 표시한다.
- 최초 예산의 상한은 선택 월 **이전의 등록된 월 전체**에 대한 카테고리별 월평균이다. 등록 월에 특정 카테고리 거래가 없으면 0원을 포함한다. 거래가 전혀 없는 월은 미등록으로 취급한다.
- 이전 월이 하나도 없으면 선택 월을 초기 기준으로 사용하고 안내한다. 기준 데이터가 없으면 상한은 0원이다.
- 월평균 합계가 200만 원보다 크면 비율에 따라 200만 원을 배정한다. 원 단위 나머지도 상한 안에서 분배한 후 각 항목의 만원 미만을 버린다. 더 적으면 각 평균의 만원 미만을 버린다. 따라서 최종 합계는 200만 원보다 적을 수 있다.
- 저장한 월별 평균과 예산은 고정한다. 이후 거래가 늘어도 과거 예산이 자동으로 바뀌지 않는다. 예산은 각 상한과 총 200만 원 안에서 수정할 수 있다.
- 원인 선택과 카테고리별 피드백 입력은 제공하지 않는다. 비교표를 먼저 보여주고 예산 편집은 접힌 영역에서 제공한다. 회고는 한 달 전체에 대해 각자 한 줄씩만 작성한다. 이전에 저장된 원인 태그·메모는 보존한다.
- 웹 비교표는 원 단위, 모바일은 만원 단위(소수 첫째 자리 반올림)로 표시한다. 계산과 저장은 원 단위를 유지하며 각 수치의 접근성 레이블에도 정확한 원 금액을 제공한다.
- 목표는 0~3개를 저장할 수 있다. 금액, 양수 순지출 거래의 결제 횟수, 특정 가맹점 결제 0회(구독 해지)를 지원한다. 현재 월까지 최근 등록 3개월의 중앙값을 참고로 표시한다.
- 선택 월에서 저장한 목표는 바로 다음 달에 평가한다. 거래가 없는 월은 성공으로 판정하지 않는다. 목표와 실적의 차이는 ‘여유/초과’이며 인과적인 절감액으로 표시하지 않는다.

## 저장과 결산

- 기존 가구 회원 전용 `advice/monthly-review-YYYY-MM` 문서를 사용한다. 기존 `firestore.rules`의 가구 UID 제한이 적용된다. 별도 공개 저장소나 브라우저 영구 저장소에는 회고를 저장하지 않는다.
- 예산·원인·목표는 Firestore transaction으로 저장하며 revision이 다르면 덮어쓰지 않고 최신 내용 불러오기를 요청한다.
- 코멘트 저장은 로그인된 본인의 UID 항목만 갱신한다. transaction 재시도로 배우자의 코멘트를 보존한다. 기존 보안 규칙은 가구 단위 접근 제어이며, 가구 구성원 사이의 필드별 보안 권한을 별도로 제한하지는 않는다.
- 두 구성원 모두 같은 예산 revision과 같은 카드 거래 내용에 대해 코멘트를 저장해야 결산 완료다. 저장 시 서버 거래를 다시 읽어 비교한다. 거래·예산·원인·목표가 변경되면 재확인이 필요하다.
- 현재 달과 미래 달은 계획을 저장할 수 있지만 월말 결산은 불가하다. 지난달도 거래가 있어야 하고, 각자 업로드 완료·실적 확인에 체크해야 한다. 실제 업로드의 완전성 자체는 자동 판별하지 않는다.
- 작성 중 월 이동 시 입력은 메모리에 유지한다. 로그아웃하면 삭제한다. 새로고침 전에 미저장 경고를 표시한다.
- 기존 분석용 요약 보관은 카테고리 설정에 있으며 별도의 보관 월을 선택한다. 결산 완료 여부를 대체하지 않는다.

## 검증

`node --test tests/*.test.cjs`로 계산, 두 사람의 확인 조건, 실제 Firebase 어댑터의 transaction 호출을 모의 저장소로 검증한다.

`node tests/monthly-review.browser.cjs`는 Playwright가 NODE_PATH에 있고 Microsoft Edge가 설치된 환경에서 실행한다. 네트워크를 차단한 합성 데이터로 320/390/768/1440px 화면, 저장 실패·재시도, 두 계정의 확인, 거래 변경, 미등록 월, 임시 입력 복원, 로그아웃 초기화를 검사한다.

실제 Firebase에 대한 쓰기나 실제 iPhone 검증은 수행하지 않았다. 배포 후 두 가구 계정으로 각각 코멘트 저장과 다른 기기 동기화를 확인할 수 있다.

월예산 입력은 천 단위 쉼표로 표시한다. 입력을 마치거나 저장할 때 만원 미만을 버린다(392,091 → 390,000). 기존 저장 예산은 저장 버튼을 눌러야 변경된다.
